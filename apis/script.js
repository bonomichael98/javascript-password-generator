let generateButton = $('#generate')

console.log(generateButton);

generateButton.on('click', function(event) {
    console.log('button works');
    console.log(event);
})

let selectDiv = $('div');

console.log(selectDiv);

//$('#generate')
//$('')

//each can iterate over objects = forEach(specifically an array method)
//forIn
//